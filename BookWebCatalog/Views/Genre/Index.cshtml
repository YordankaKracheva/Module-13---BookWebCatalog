@using BookWebCatalog.Data.Models;
@using static BookWebCatalog.Common.AdminUser;
@model List<Genre>

<h2 class="text-center mb-5 heading">List of @Model.Count Genres</h2>

<div class="row mb-5 justify-content-between">
    <div class="col">
        @if (User.IsInRole(AdminRoleName))
        {
            <a class="btn btn-cta" asp-area="" asp-controller="Genre" asp-action="Create">New Genre</a>
        }
    </div>
</div>


<div class="table-responsive">
    <table class="table table-striped table-hover">
        <thead class="thead-dark">
            <tr>
                <th>Id</th>
                <th>Name</th>
                @if (User.IsInRole(AdminRoleName))
                {
                    <th>Actions</th>
                }
            </tr>
        </thead>
        <tbody>
            @foreach (var genre in Model)
            {
                <tr>
                    <td>@genre.Id</td>
                    <td>@genre.Name</td>
                    @if (User.IsInRole(AdminRoleName))
                    {
                        <td class="actions" style="white-space:nowrap">
                            <a class="btn btn-sm btn-outline-primary" asp-controller="Genre" asp-action="Edit" asp-route-id="@genre.Id">Edit</a>
                            <a class="btn btn-sm btn-outline-danger" asp-controller="Genre" asp-action="Delete" asp-route-id="@genre.Id" onclick="return confirm('Are you sure you want to delete this genre?')">Delete</a>
                        </td>
                    }
                </tr>
            }
        </tbody>
    </table>
</div>

<style>
    body {
        font-family: 'Poppins', sans-serif;
        background: linear-gradient(to right, #6f4f26, #d2b48c);
        margin: 0;
        padding: 0;
    }

    h2.heading {
        font-size: 1.75rem; /* Adjusted to be more readable */
        color: #fff;
        font-weight: 700;
        margin-bottom: 20px;
        text-shadow: 2px 2px 5px rgba(0, 0, 0, 0.3);
    }

    .btn-cta {
        background-color: #432818;
        border: none;
        color: white;
        font-weight: 600;
        border-radius: 5px;
        padding: 10px 20px;
        transition: background-color 0.3s ease;
    }

        .btn-cta:hover {
            background-color: #582f0e;
        }

    .table {
        margin-top: 20px;
        border-collapse: collapse;
        width: 100%; /* Full width but controlled by max-width */
        max-width: 80%; /* Makes the table width responsive and more suitable */
        margin-left: auto;
        margin-right: auto;
        border-radius: 8px;
        table-layout: fixed; /* Ensures equal column width */
    }

        .table th, .table td {
            padding: 12px 16px; /* Slightly larger padding for better readability */
            text-align: left;
            font-size: 1rem; /* Optimal size for table content */
            word-wrap: break-word; /* Ensure text wraps in columns */
        }

    .thead-dark {
        background-color: #6f4f26;
        color: white;
    }

    .table td {
        background-color: #f9f9f9;
        border-top: 1px solid #ddd;
    }

    .table-hover tbody tr:hover {
        background-color: #e9e9e9;
    }

    .btn-primary {
        background-color: #8b5e3c;
        border: none;
        color: white;
        font-weight: 600;
        border-radius: 5px;
        padding: 8px 16px;
        transition: background-color 0.3s ease;
    }

        .btn-primary:hover {
            background-color: #5a3e2d;
        }

    .btn-outline-primary, .btn-outline-danger {
        border: 1px solid #8b5e3c;
        color: #8b5e3c;
        font-weight: 600;
        border-radius: 5px;
        padding: 6px 12px;
        transition: background-color 0.3s ease, color 0.3s ease;
    }

        .btn-outline-primary:hover, .btn-outline-danger:hover {
            background-color: #8b5e3c;
            color: white;
        }

    .actions {
        white-space: nowrap;
    }

    media (max-width: 767px) {
        h2 .heading

    {
        font-size: 1.5rem; /* Smaller font size on mobile */
    }

    .table th, .table td {
        padding: 8px 10px; /* Slightly smaller padding on mobile */
        font-size: 0.9rem; /* Adjust font size for mobile view */
    }

    .btn-primary {
        font-size: 0.9rem;
        padding: 6px 12px;
    }

    .btn-outline-primary, .btn-outline-danger {
        font-size: 0.9rem;
        padding: 4px 10px;
    }

    }
    .form-control {
        width: 300px;
        display: inline-block;
    }

    .btn-primary {
        background-color: #8b5e3c;
        border: none;
        color: white;
        font-weight: 600;
        border-radius: 5px;
        padding: 8px 16px;
        transition: background-color 0.3s ease;
    }
</style>
